// 冒泡排序
template<typename T>
void bubbleSort(T *arr, int len);

// 插入排序
template<typename T>
void insertSort(T *arr, int len);

// 选择排序
template<typename T>
void selectSort(T *arr, int len);

// 快速排序
template<typename T>
void quickSort(T *arr, int len);

// 基数排序(桶排序)
template<typename T>
void radixSort(T *arr, int len);

// 希尔排序
template<typename T>
void shellSort(T *arr, int len);

// 归并排序
template<typename T>
void mergeSort(T *arr, int len);

// 竞标赛排序
template<typename T>
void contestSort(T *arr, int len);

// 堆排序
template<typename T>
void heapSort(T *arr, int len);
